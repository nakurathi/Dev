apiVersion: v1
kind: Service
metadata:
  name: gcp-springboot-svc
  namespace: default
  labels:
    app: gcp-springboot
spec:
  type: LoadBalancer
  selector:
    app: gcp-springboot
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
