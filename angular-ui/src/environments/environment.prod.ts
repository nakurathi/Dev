// src/environments/environment.prod.ts — PRODUCTION (GKE)
export const environment = {
  production: true,
  apiUrl: 'https://api.healthcare.example.com',  // replace with your GKE LoadBalancer IP/domain
};
